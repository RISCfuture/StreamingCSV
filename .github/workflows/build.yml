name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-macos:
    name: Build on macOS
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-15]
        xcode: ["26.1"]
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_${{ matrix.xcode }}.app

      - name: Show Swift version
        run: swift --version

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        run: swiftlint --strict

      - name: Build
        run: swift build -v

      - name: Run tests
        run: swift test -v

  build-linux:
    name: Build on Linux
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        swift: ["6.2"]
    container:
      image: swift:${{ matrix.swift }}
    steps:
      - uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build
        run: swift build -v

      - name: Run tests
        run: swift test -v

  # Windows CI is temporarily disabled due to Swift installer URL issues
  # The compnerd/gha-setup-swift action constructs incorrect URLs for Swift 6.1.2
  # Expected: https://download.swift.org/swift-6.1.2-release/windows10/swift-6.1.2-RELEASE/swift-6.1.2-RELEASE-windows10.exe
  # Actual: https://download.swift.org/6.1.2/windows10/swift-6.1.2-RELEASE/swift-6.1.2-RELEASE-windows10.exe
  # 
  # build-windows:
  #   name: Build on Windows
  #   runs-on: windows-latest
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       swift-version: ["6.1.2"]
  #       swift-build: ["6.1.2-RELEASE"]
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Install Swift
  #       uses: compnerd/gha-setup-swift@main
  #       with:
  #         swift-version: ${{ matrix.swift-version }}
  #         swift-build: ${{ matrix.swift-build }}

  #     - name: Show Swift version
  #       run: swift --version

  #     - name: Build
  #       run: swift build -v

  #     - name: Run tests
  #       run: swift test -v

  swift-format:
    name: Run swift-format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: SwiftyLab/setup-swift@latest
        with:
          swift-version: "6.2"
      - name: Download .swift-format config
        run: curl -o .swift-format https://gist.githubusercontent.com/RISCfuture/e0c21afb7bd80a88d128a42bf40d2ecd/raw/.swift-format
      - name: Install swift-format
        run: |
          git clone https://github.com/swiftlang/swift-format.git
          cd swift-format
          swift build -c release
          sudo cp .build/release/swift-format /usr/local/bin/
      - name: Run swift-format lint
        run: swift-format lint -r .
